
// Assuming this is in a file named something like dbConfig.js

// Exporting a constant
// module.exports.DB_NAME = "USERS";

// Assuming this is in a file named something like dbConfig.js

// Exporting the constant directly
module.exports = {
    DB_NAME: "USERS"
};

